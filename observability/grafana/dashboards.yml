apiVersion: 1

providers:
  - name: SpanReplay
    orgId: 1
    folder: Production AI
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
